var operators = new Array(['+','-','*','/']);
while(true){
    var i = 0;
    var score = 0;
    while(i < 10){
       firstNum = round(Math.random()*100)
       secondNum = round(Math.random*100)
       opNum = round(Math.random()*3);
       console.log(` ${firstNum} ${operators[opNum]} ${})
    }

}